import React from 'react';
import { Meta } from '@storybook/addon-docs/blocks';
import { useLanguageStore } from '../../src/stores/languageStore'
import { LanguageSwitcher } from "../components/LanguageSwitcher";
import styles from './styles/index.module.css'

<Meta title="Component Overview" />

export const ComponentOverview = () => {
  const language = useLanguageStore(state => state.language);

  const texts = {
    en: {
      title: "Component Overview",
      description:
        "IsskinUI offers a comprehensive collection of React components crafted for consistency, accessibility, and ease of use. From buttons to modals and inputs to layout primitives, every component is thoughtfully designed to match our design system and seamlessly integrate into our apps.",
      cards: [
        // {
        //   component: '',
        //   name: ''
        // },
      ],
    },
    pt: {
      title: "Visão Geral dos Componentes",
      description:
        "O IsskinUI oferece uma coleção abrangente de componentes React projetados com foco em consistência, acessibilidade e facilidade de uso. De botões a modais, de inputs a elementos de layout, cada componente segue fielmente o nosso design system e se integra com fluidez aos nossos apps.",
      cards: [
        // {
        //   component: '',
        //   name: ''
        // },
      ],
    }
};

  
  const t = texts[language];

  return (
    <div className={styles.container}>
      <div className={styles.sectionTitle}>
        <h1>{t.title}</h1>

        <p>{t.description}</p>        
      </div>

      <div className={styles.section}>
        <div className={styles.cardGrid}>
          {texts[language].cards.map((card, index) => (
            <a href="#" rel="noopener noreferrer" key={index}>
              <div className={styles.card}>
                <div className={styles.cardComponent}>
                  {card.component}
                </div>
                <p>{card.name}</p>
              </div>
            </a>
          ))}
        </div>
      </div>
    </div>
  );
};

<LanguageSwitcher/>

<ComponentOverview />
```

        ```